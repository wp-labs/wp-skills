#[copy_raw(name:"raw_msg")]
package vaultwarden {
  #[tag(log_desc: "Vaultwarden 请求日志", log_type: "vaultwarden_request")]
  rule vaultwarden_request {
    (
      time:timestamp<[,]>,
      chars:vw_module<[,]>,
      chars:level<[,]>,
      http/method:method,
      chars:path
    )
  }
}
