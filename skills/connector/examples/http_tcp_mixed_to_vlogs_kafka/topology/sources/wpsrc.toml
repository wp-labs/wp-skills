[[sources]]
key = "http_mixed_logs"
enable = true
connect = "http_src"
tags = ["transport:http", "port:19011", "source:mixed"]

[sources.params]
port = 19011
path = "/ingest"

[[sources]]
key = "tcp_mixed_logs"
enable = true
connect = "tcp_src"
tags = ["transport:tcp", "port:19012", "source:mixed"]

[sources.params]
port = 19012
framing = "auto"
prefer_newline = true
