#[copy_raw(name:"raw_msg")]
package vaultwarden {
  #[tag(log_desc:"Vaultwarden request log", log_type:"vaultwarden_request")]
  rule vaultwarden_request {
    (
      time:request_time<[,]>,
      chars:logger<[,]>,
      chars:level<[,]>,
      http/method:method,
      chars:path
    )
  }
}
